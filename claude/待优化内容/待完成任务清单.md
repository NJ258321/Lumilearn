# 溯光智习 (LumiTrace AI) 待完成任务清单

> 生成时间: 2026-03-09
> 说明：综合考量时间限制，筛选出真正需要实现的核心任务

---

## 一、任务完成情况总览

根据代码检查，项目已完成约 **70%** 的功能。前端17个页面基本完成，后端28个API路由已实现。当前需要完成的是**核心功能的增强和完善**。

---

## 二、前端待完成任务

### 2.1 高优先级（必须完成）

| 编号 | 任务名称 | 文件 | 说明 |
|------|----------|------|------|
| FE-01 | Tailwind CSS 配置 | 需新建 `tailwind.config.js` | 项目使用Tailwind但缺少配置，会导致样式无法正确编译 |
| FE-02 | 依赖问题修复 | `package.json` | `@reduxjs/toolkit` 声明缺失，需移除或添加依赖 |
| FE-03 | 知识图谱可视化增强 | `src/components/KnowledgeGraph.tsx` | 节点点击交互、状态颜色区分、关系边交互 |
| FE-04 | 多模态时间轴同步 | `pages/TimeMachine.tsx` | 音频-文字毫秒级同步、PPT自动翻页、进度条标记点 |
| FE-05 | 手写笔迹回放 | `src/components/TranscriptionPanel.tsx` | Canvas笔迹动画、与音频同步播放 |
| FE-06 | 备考日历页面 | `pages/ExamCalendar.tsx`（新建） | 三轮复习时间轴、月历视图、进度图表 |
| FE-07 | Timeline 标记点交互 | `src/components/Timeline.tsx` | 重点/板书/错题标记点击交互 |
| FE-08 | 环境变量配置 | 需新建 `.env.local`、`.env.example` | API环境变量配置 |

### 2.2 中优先级（建议完成）

| 编号 | 任务名称 | 文件 | 说明 |
|------|----------|------|------|
| FE-09 | Dashboard 备考入口 | `pages/Dashboard.tsx` | 添加备考日历入口按钮 |
| FE-10 | 空状态页面完善 | 各页面 | 首页/采集页/课程页/错题本空状态引导 |
| FE-11 | 移动端手势优化 | 通用 | 知识图谱双指缩放、左右滑动切换 |
| FE-12 | 错误处理完善 | 通用 | 网络错误重试、加载失败处理、退出登录清除缓存 |

### 2.3 低优先级（暂不实现）

以下任务因时间有限，**暂不实现**：
- "光之汇聚"动画效果
- 深色模式支持
- 性能优化（虚拟列表、懒加载）
- 辅助功能（屏幕阅读器、键盘导航）
- 构建配置完善（ESLint、Prettier）

---

## 三、后端待完成任务

### 3.1 高优先级（必须完成）

| 编号 | 任务名称 | 文件/路由 | 说明 |
|------|----------|-----------|------|
| BE-01 | 知识点状态API | `routes/knowledge-points.ts` | 扩展获取知识点掌握状态字段 |
| BE-02 | 音频同步数据API | `routes/audio.ts` | 新增文字时间戳、PPT切换时间点数据接口 |
| BE-03 | 笔迹数据API | `routes/audio.ts` | 新增笔迹坐标数据接口 |
| BE-04 | 复习日历API | `routes/review.ts` | 新增三轮复习时间轴数据接口 |
| BE-05 | 复习进度API | `routes/review.ts` | 新增各轮复习进度数据接口 |
| BE-06 | 短板发现算法 | 需新建服务 | 知识图谱前置概念缺失检测、高频引用低覆盖检测 |
| BE-07 | 缺口检测API | `routes/knowledge-relations.ts` | POST `/api/knowledge-graph/detect-gaps` |
| BE-08 | 薄弱点分析API | `routes/knowledge-relations.ts` | POST `/api/knowledge-graph/weakness-analysis` |
| BE-09 | 智能备考规划API | `routes/planning.ts`（新建） | POST `/api/planning/optimize` 路径优化 |
| BE-10 | 备考规划算法 | 需新建服务 | 复习计划优化、状态建模、目标函数定义 |

### 3.2 中优先级（建议完成）

| 编号 | 任务名称 | 说明 |
|------|----------|------|
| BE-11 | 测试框架搭建 | Jest、Supertest、ts-jest 配置 |
| BE-12 | 课程/章节/知识点测试 | 基础API测试 |
| BE-13 | 请求频率限制 | Rate Limiting 配置 |
| BE-14 | 文件删除功能 | 实现文件删除API |

### 3.3 低优先级（暂不实现）

以下任务因时间有限，**暂不实现**：
- 众源资源检索（B站/Wiki抓取）
- Redis缓存层
- 日志系统和告警机制
- Docker容器化
- CI/CD配置
- PostgreSQL迁移
- 对象存储配置

---

## 四、任务依赖关系

### 前端依赖后端的关键路径

```
后端API准备                    前端实现
    │                            │
BE-01 (知识点状态) ──────────► FE-03 (知识图谱)
    │                            │
BE-02 (音频同步) ──────────► FE-04 (多模态同步)
    │                            │
BE-03 (笔迹数据) ──────────► FE-05 (笔迹回放)
    │                            │
BE-04+BE-05 (复习数据) ──────► FE-06 (备考日历)
```

### 建议开发顺序

**第一阶段（1-2天）：配置修复**
- FE-01 Tailwind配置
- FE-02 依赖修复
- FE-08 环境变量配置

**第二阶段（3-5天）：核心功能后端API**
- BE-01 ~ BE-05 API扩展
- BE-06 ~ BE-10 算法和API

**第三阶段（3-5天）：前端核心功能**
- FE-03 知识图谱增强
- FE-04 多模态时间轴
- FE-05 手写笔迹回放
- FE-06 备考日历页面
- FE-07 Timeline交互

**第四阶段（2-3天）：完善优化**
- FE-09 ~ FE-12 中优先级任务

---

## 五、任务统计

| 类别 | 高优先级 | 中优先级 | 低优先级 | 合计 |
|------|----------|----------|----------|------|
| 前端 | 8 | 4 | 0 | 12 |
| 后端 | 10 | 4 | 0 | 14 |
| **总计** | **18** | **8** | **0** | **26** |

---

## 六、不再实现的内容

以下原计划内容经综合考量后**不再实现**：

### 前端
- 深色模式
- 动画效果（光之汇聚）
- 性能优化（虚拟列表等）
- 辅助功能
- 完整ESLint/Prettier配置

### 后端
- 众源资源检索（B站/Wiki）
- Redis缓存
- 日志/告警系统
- Docker容器化
- CI/CD
- 生产环境配置（PostgreSQL/OSS）
- 数据迁移脚本

---

*此文档将作为后续开发的指导文件*
